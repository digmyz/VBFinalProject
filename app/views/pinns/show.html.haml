= image_tag @pinn.image.url(:medium)
%h1= @pinn.title
%p= @pinn.description
%p
Submitted by
= @pinn.user.email

%br/
//%p= @pinn.user ? @pinn.user.email : "No User Assigned"

= link_to "Back", root_path
= link_to "Edit", edit_pinn_path
= link_to "Delete", pinn_path, method: :delete, data: { confirm: "Are you sure?" } 